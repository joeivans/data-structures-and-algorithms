'use strict';

const PseudoQueue = require('../index');

describe('PseudoQueue Tests', function () {
  it('should ..', function () {
    // Given
    const queue = new PseudoQueue();

    // When

    // Then
  });
});
