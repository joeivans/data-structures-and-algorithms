const { Graph } = require('../graph/graph');

/**
 * Calculates the cost of direct flights or returns null.
 * @param graph The graph to traverse.
 * @param citiesArr The city names to check paths.
 * @returns {number | null} Returns the cost of direct flights or null if no direct flights.
 */
function graphBusinessTrip(graph, citiesArr) {
}

module.exports = {
  Graph,
  graphBusinessTrip
};
